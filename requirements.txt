# Python 依赖 - my-wiki

# HTTP 请求（用于 RSS 抓取和 GitHub API）
requests>=2.28.0

# HTML 解析（用于 RSS 抓取）
beautifulsoup4>=4.11.0

# Markdown 处理
markdown>=3.4.0

# YAML 处理（用于 frontmatter）
pyyaml>=6.0

# 日期时间处理
python-dateutil>=2.8.0

# 可选：用于更高级的 Markdown 处理
# mistune>=2.0.0

# 可选：用于 Obsidian dataview 查询
# dataclasses-json>=0.5.0

# 桌面 GUI（wiki_app.py 等，PySide6 优先，PyQt6 可替代）
PySide6>=6.5.0

# OpenClaw 集成
# openclaw-client>=0.1.0  # 如果有的话

# ---- 面部情绪识别（face_mood.py，可选） ----
# MediaPipe FaceMesh 面部关键点检测 + 几何特征 → 心情映射
# 安装后 mood_web.html 的「📷 面部」按钮可用
# mediapipe>=0.10.0       # 首次约 50MB，内置 FaceMesh 模型，离线可用
# opencv-python>=4.8.0    # 摄像头采样

# ---- 语音识别（voice_mood.py，可选） ----
# SpeechRecognition>=3.10  # Google Web Speech API（需联网）
# static-ffmpeg            # 无系统 ffmpeg 时的纯 pip 替代
